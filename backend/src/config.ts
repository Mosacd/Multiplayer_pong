import { GameConfig } from './types';

export const GAME_CONFIG: GameConfig = {
  FIELD_WIDTH: 800,
  FIELD_HEIGHT: 400,
  PADDLE_HEIGHT: 80,
  PADDLE_WIDTH: 10,
  BALL_SIZE: 10,
  BALL_INITIAL_SPEED: 5,
  PADDLE_SPEED: 8,
  WINNING_SCORE: 5
};